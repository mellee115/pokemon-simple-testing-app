/// <reference types="react" />
import { Option } from "../lib/interfaces";
interface ISelectItemProps {
    itemRenderer: any;
    option: Option;
    checked: boolean;
    focused?: boolean;
    disabled?: boolean;
    onSelectionChanged: (checked: boolean) => void;
    onClick: any;
}
declare const SelectItem: ({ itemRenderer: ItemRenderer, option, checked, focused, disabled, onSelectionChanged, onClick, }: ISelectItemProps) => JSX.Element;
export default SelectItem;
